One directory per finished work, named with its Latin slug.

Create them with `tools/new-work.sh`, never by hand — the script stamps the
licence header from `author.conf`, so the author's name and the licence cannot
drift between one piece and the next.
